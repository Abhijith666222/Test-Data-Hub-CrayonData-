CREATE TABLE credit_card_accounts (
    rim_no VARCHAR(24),
    card_number VARCHAR(24),
    primarycardserno VARCHAR(24),
    accounttype VARCHAR(16),
    status VARCHAR(16),
    billingmethod VARCHAR(16),
    product_name VARCHAR(32),
    currency VARCHAR(8),
    creditlimit DECIMAL(10,2),
    balance DECIMAL(10,2),
    totalpoints INT,
    totalpointtrxns INT,
    overdueamount DECIMAL(10,2),
    overduedate DATE,
    minamtduedate DATE,
    minamount DECIMAL(10,2),
    totalcredits DECIMAL(10,2),
    totaldebits DECIMAL(10,2),
    lastbillingdate DATE,
    lastpaymentserno VARCHAR(32),
    lastpaymentpartitionkey VARCHAR(32),
    instalmentcount INT,
    instalmentbalance DECIMAL(10,2),
    instalmentbalanceprincipal DECIMAL(10,2),
    acqinstbalance DECIMAL(10,2),
    cashbalance DECIMAL(10,2),
    createdate DATE,
    closedate DATE,
    excesspaymentamount DECIMAL(10,2),
    latepaymentfee DECIMAL(10,2),
    next_payment_due_date DATE,
    autopay_enabled BOOLEAN,
    linked_mobile_number VARCHAR(8),
    card_network VARCHAR(16),
    is_primary_card BOOLEAN,
    card_expiry VARCHAR(7),
    card_block_reason VARCHAR(64),
    max_credit_limit DECIMAL(10,2),
    max_daily_spend_limit DECIMAL(10,2)
);
